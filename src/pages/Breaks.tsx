import BottomNav from "@/components/BottomNav";

const Breaks = () => (
  <div className="min-h-screen bg-background pb-20 px-4 pt-6">
    <h1 className="text-2xl font-heading font-bold text-foreground mb-2">Navigator Breaks</h1>
    <p className="text-muted-foreground">Live and upcoming case breaks — coming soon.</p>
    <BottomNav />
  </div>
);

export default Breaks;
