import BottomNav from "@/components/BottomNav";

const Profile = () => (
  <div className="min-h-screen bg-background pb-20 px-4 pt-6">
    <h1 className="text-2xl font-heading font-bold text-foreground mb-2">Profile</h1>
    <p className="text-muted-foreground">Your profile and settings — coming soon.</p>
    <BottomNav />
  </div>
);

export default Profile;
