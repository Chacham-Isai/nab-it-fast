import BottomNav from "@/components/BottomNav";

const GrabBags = () => (
  <div className="min-h-screen bg-background pb-20 px-4 pt-6">
    <h1 className="text-2xl font-heading font-bold text-foreground mb-2">Grab Bags</h1>
    <p className="text-muted-foreground">Mystery boxes curated to your taste — coming soon.</p>
    <BottomNav />
  </div>
);

export default GrabBags;
