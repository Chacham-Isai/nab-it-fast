import BottomNav from "@/components/BottomNav";

const Auctions = () => (
  <div className="min-h-screen bg-background pb-20 px-4 pt-6">
    <h1 className="text-2xl font-heading font-bold text-foreground mb-2">Live Auctions</h1>
    <p className="text-muted-foreground">Real-time authenticated auctions — coming soon.</p>
    <BottomNav />
  </div>
);

export default Auctions;
