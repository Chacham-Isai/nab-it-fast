import BottomNav from "@/components/BottomNav";

const Play = () => (
  <div className="min-h-screen bg-background pb-20 px-4 pt-6">
    <h1 className="text-2xl font-heading font-bold text-foreground mb-2">Navigator Play</h1>
    <p className="text-muted-foreground">Breaks · Grab Bags · Auctions — coming soon.</p>
    <BottomNav />
  </div>
);

export default Play;
